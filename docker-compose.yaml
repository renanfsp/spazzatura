services:
  database:
    image: postgres:16.8
    restart: always
    volumes:
      - $HOME/.docker/volumes/postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=spazzatura
      - POSTGRES_PASSWORD=spazzatura
    ports:
      - 5432:5432
